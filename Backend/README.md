# Mechanic Backend

Mechanic backend ka actual code ab is folder ke andar available hai.

- `controllers/` mechanic controllers
- `routes/` mechanic routes
- `config/`, `db.js`, `middleware/`, `utils/`, `init/` required backend support files
- `index.js` mechanic backend server entry
- `package.json` mechanic backend package config
