# Customer Backend

Customer backend ka actual code ab is folder ke andar available hai.

- `controllers/` customer controllers
- `routes/` customer routes
- `config/`, `db.js`, `middleware/`, `utils/`, `init/` required backend support files
- `index.js` customer backend server entry
- `package.json` customer backend package config
